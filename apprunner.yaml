version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r requirements.txt
run:
  command: python3 -m gunicorn app:app --bind 0.0.0.0:8080 --timeout 300 --workers 1
  network:
    port: 8080
  env:
    - name: PYTHONPATH
      value: /app
